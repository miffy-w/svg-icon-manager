{
  "name": "svg-icon-manager",
  "displayName": "Image Asset Manager",
  "description": "Scan, preview, and manage all image assets (SVG, PNG, JPG, WebP, GIF, ICO, BMP) in your workspace with a beautiful card-based gallery",
  "version": "1.1.5",
  "publisher": "svg-icon-manager",
  "author": "miffy-w",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/miffy-w/svg-icon-manager"
  },
  "bugs": {
    "url": "https://github.com/miffy-w/svg-icon-manager/issues"
  },
  "homepage": "https://github.com/miffy-w/svg-icon-manager#readme",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other",
    "Visualization",
    "Images"
  ],
  "keywords": [
    "svg",
    "icon",
    "image",
    "asset",
    "preview",
    "gallery",
    "manager",
    "png",
    "jpg",
    "webp",
    "gif",
    "ico",
    "bmp",
    "assets"
  ],
  "icon": "resources/icon.png",
  "activationEvents": [
    "onCommand:svgIconManager.show"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "svgIconManager.show",
        "title": "Show Image Asset Manager",
        "category": "Image Asset Manager"
      },
      {
        "command": "svgIconManager.refresh",
        "title": "Refresh Assets",
        "category": "Image Asset Manager"
      }
    ],
    "keybindings": [
      {
        "command": "svgIconManager.show",
        "key": "ctrl+alt+s",
        "mac": "cmd+alt+s"
      }
    ],
    "configuration": {
      "title": "Image Asset Manager",
      "properties": {
        "svgIconManager.ignorePatterns": {
          "type": "array",
          "default": [
            "node_modules",
            ".git",
            "out",
            "dist",
            "build",
            "coverage"
          ],
          "description": "Directory patterns to ignore when scanning for image files"
        },
        "svgIconManager.iconSize": {
          "type": "number",
          "default": 80,
          "minimum": 48,
          "maximum": 128,
          "description": "Size of asset preview in pixels"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "package": "npx vsce package",
    "publish": "npx vsce publish"
  },
  "devDependencies": {
    "@types/image-size": "^0.7.0",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.74.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@vscode/vsce": "^2.22.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "image-size": "^2.0.2",
    "minimatch": "^10.2.5"
  }
}
